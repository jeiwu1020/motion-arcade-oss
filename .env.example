# Client-visible, non-secret build flags. VITE_* values are bundled into browser code.
VITE_ENABLE_TEST_INPUT=false

# Developer-only real camera and Pose Sensor Lab. Development enables it by default.
# Production builds require the exact value true.
VITE_ENABLE_REAL_SENSOR_LAB=false
